CARMIND VOSK ANDROID BUILD FIX v1.6.3

Upload these TWO files to the ROOT of the GitHub repository:

1. Carmind-project.zip
2. codemagic.yaml

Replace the previous files. Do not extract Carmind-project.zip. Start a new
CodeMagic build from the main branch after both files finish uploading.

Version 1.6.3 fixes the CodeMagic Java error:
"class file for com.sun.jna.PointerType not found".

Both Carmind Car and Carmind Mobile now include Vosk's required Android JNA
runtime. The clean CodeMagic build directory, restored Vosk microphone engine,
permissions, Nigerian-English command matching and British response voice are
preserved.

The debug build produces:

- app-debug.apk — Android car screen
- phone-debug.apk — Android phone
