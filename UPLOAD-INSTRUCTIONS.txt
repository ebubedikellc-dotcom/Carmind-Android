CARMIND CLEAN GITHUB BUILD v1.6.2

Emeka must upload these TWO files to the ROOT of the GitHub repository:

1. Carmind-project.zip
2. codemagic.yaml

Replace files with the same names. Do not extract Carmind-project.zip and do
not upload its folders separately. Confirm both filenames are visible on the
first page of the repository, then start a new CodeMagic build from main.

The CodeMagic workflow now deletes its temporary Carmind build directory and
extracts this release into a clean directory on every build. Old GitHub source
files therefore cannot mix with this version or break compilation. It checks
for package.json, package-lock.json and both Android app modules before npm ci.

The successful debug workflow produces TWO artifacts:

- app-debug.apk — Carmind Car for an Android car screen
- phone-debug.apk — Carmind Mobile for an Android phone

Install only the newly generated v1.6.2 APK. Approve Microphone first, followed
by Contacts, Phone, Music/Audio, Photos/Videos, Location, Nearby Devices and
Notifications when Android asks.
