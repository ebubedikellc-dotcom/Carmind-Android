CARMIND AI — COMPLETE GITHUB UPLOAD

This package already contains the complete repository at its root.

1. Create a new empty GitHub repository, or completely empty the existing repository.
2. Extract this ZIP on the phone or computer.
3. Upload every extracted file and folder to the ROOT of the GitHub repository.
4. Do not upload this ZIP as a single file.
5. Do not place the extracted files inside another folder.
6. Before starting CodeMagic, confirm these are visible at the repository root:
   codemagic.yaml
   package.json
   package-lock.json
   android/
   scripts/
   src/
7. In CodeMagic, select the main branch and run "Carmind AI - Installable Debug APK".

IMPORTANT:
The workflow builds directly from the GitHub repository. It does not search for,
open, or unzip a file named Carmind-project.zip.
